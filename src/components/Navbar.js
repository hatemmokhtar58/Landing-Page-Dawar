export default function Navbar() {
  return `
    <nav class="fixed w-full z-50 top-0 right-0 p-4 md:p-6 pointer-events-none">
      <div class="container mx-auto flex justify-between items-center">
        <!-- Logo (Right) -->
        



      </div>
    </nav>
  `;
}
